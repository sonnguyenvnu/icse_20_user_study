/** 
 * Disable the function tagging wrappers in the  {@code tagX} methods.<p> Note that disabling doesn't remove the wrappers from previously tagged functions.
 */
public static void disable(){
  enabled=false;
}
