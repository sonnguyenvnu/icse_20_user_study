/** 
 * Aha! This seems to prove that whenever a node may start with "LOCAL",   the "LOCAL" token is put in the array zero and the rest of the tokens   are put in the array one.                                              
 */
public final boolean local(){
  return zero != null;
}
