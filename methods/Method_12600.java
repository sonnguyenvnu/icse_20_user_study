/** 
 * ??segmentBuff????????? ???????? 1.available == BUFF_SIZE ??buffer?? 2.buffIndex < available - 1 && buffIndex > available - BUFF_EXHAUST_CRITICAL???????????? 3.!context.isBufferLocked()????segmenter???buffer ????????buffer????????????????
 * @return
 */
boolean needRefillBuffer(){
  return this.available == BUFF_SIZE && this.cursor < this.available - 1 && this.cursor > this.available - BUFF_EXHAUST_CRITICAL && !this.isBufferLocked();
}
