/** 
 * ????????? key-value ?? ?????? key ??????? MSET ?????????????????????????????? MSETNX ??????????? key ??????????????? MSET ??????(atomic)??????? key ???????????????? key ????????? key ?????????????? <pre> ??? Cache cache = RedisKit.use();			// ?? JbootRedis ? cache cache.mset("k1", "v1", "k2", "v2");		// ???? key value ??? List list = cache.mget("k1", "k2");		// ???????????????? </pre>
 */
public String mset(Object... keysValues){
  if (keysValues.length % 2 != 0)   throw new IllegalArgumentException("wrong number of arguments for met, keysValues length can not be odd");
  byte[][] kv=new byte[keysValues.length][];
  for (int i=0; i < keysValues.length; i++) {
    if (i % 2 == 0)     kv[i]=keyToBytes(keysValues[i]);
 else     kv[i]=valueToBytes(keysValues[i]);
  }
  return jedisCluster.mset(kv);
}
