/** 
 * Return the fingerprint of the contents of the stream <code>is</code>. <code>IOException</code> is thrown in the event of an error reading the stream. 
 */
public static long New(InputStream is) throws IOException {
  return Extend(IrredPoly,is);
}
