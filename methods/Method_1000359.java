/** 
 * ?????????.
 * @param actived ???????,???????????,???null
 * @param locked  ???????,???????????,???null
 * @param ignoreNull ??????
 * @return ??/?????
 */
public static FieldMatcher make(String actived,String locked,boolean ignoreNull){
  FieldMatcher fm=new FieldMatcher();
  fm.ignoreNull=ignoreNull;
  if (!Strings.isBlank(actived))   fm.actived=Regex.getPattern(actived);
  if (!Strings.isBlank(locked))   fm.locked=Regex.getPattern(locked);
  return fm;
}
