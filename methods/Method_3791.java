/** 
 * Returns true if the scroll offset and scroll range calculations take account of span information. See  {@link #setUsingSpansToEstimateScrollbarDimensions(boolean)} for moreinformation on this topic. Defaults to  {@code false}.
 * @return true if the scroll offset and scroll range calculations take account of spaninformation.
 */
public boolean isUsingSpansToEstimateScrollbarDimensions(){
  return mUsingSpansToEstimateScrollBarDimensions;
}
