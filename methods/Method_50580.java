/** 
 * ?????????SuperTextView?StateDrawable? ???????????????????????????? {@link com.coorchice.library.image_engine.Engine}? ???? {@link ImageEngine#install(Engine)}?SuperTextView?ImageEngine??????????????? {@link Application#onCreate()}???????????????????????????????? ???????????SuperTextView??????????????????????
 * @param url          ??????
 * @param asBackground ??????????Background?- false???????????SuperTextView?StateDrawable - true????????????????? {@link SuperTextView#setDrawableAsBackground(boolean)}?????
 * @return SuperTextView
 */
public SuperTextView setUrlImage(final String url,final boolean asBackground){
  ImageEngine.checkEngine();
  curImageUrl=url;
  ImageEngine.load(url,new ImageEngine.Callback(){
    @Override public void onCompleted(    final Drawable drawable){
      if (getContext() != null && drawable != null && TextUtils.equals(curImageUrl,url)) {
        SuperTextView.this.drawable=drawable;
        isShowState=!asBackground;
        setDrawableAsBackground(asBackground);
      }
    }
  }
);
  return this;
}
