/** 
 * Whether the preprocessor found a main() method. If main() is found, then it will be used to launch the sketch instead of PApplet.main().
 */
public boolean getFoundMain(){
  return foundMain;
}
