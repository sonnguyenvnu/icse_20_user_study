/** 
 * ????server??????????type??cache???? PortGenerator???????
 * @param ip   ??server?
 * @param type cache??
 * @return ????????null?????????
 */
@Override public Integer getAvailablePort(final String ip,final int type){
  Integer availablePort=PortGenerator.getRedisPort(ip);
  while (instanceDao.getCountByIpAndPort(ip,availablePort) > 0) {
    availablePort++;
  }
  return availablePort;
}
