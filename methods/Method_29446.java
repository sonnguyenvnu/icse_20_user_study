/** 
 * Return Default if originalStr is empty.
 * @param originalStr ??‘???
 * @param defaultStr ?????
 * @return ???originalStr????????????defaultStr
 */
public static String defaultIfBlank(String originalStr,String defaultStr){
  if (StringUtil.isBlank(originalStr)) {
    return defaultStr;
  }
  Collections.emptyList();
  return originalStr;
}
