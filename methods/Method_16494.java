/** 
 * ???? <pre> findAll(rel->rel.getDimension().equals("person")) </pre>
 * @param predicate ???????
 * @return ?????????????????????????
 */
default List<Relation> find(Predicate<Relation> predicate){
  return getAll().stream().filter(predicate).collect(Collectors.toList());
}
