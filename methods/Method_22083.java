/** 
 * ????????????????????????.
 * @param items ??????????????
 * @return ????????
 */
public boolean misfireIfHasRunningItems(final Collection<Integer> items){
  if (!hasRunningItems(items)) {
    return false;
  }
  setMisfire(items);
  return true;
}
