/** 
 * Returns the backing configuration as a  {@link ReadConfiguration} that can be usedto create and configure a JanusGraph graph.
 * @return
 */
public ReadConfiguration getConfiguration(){
  return config.getConfiguration();
}
